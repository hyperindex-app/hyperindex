# Data files committed to repo for static hosting (see ROADMAP.md)
# data/index_latest.json  <- tracked
# data/history.json       <- tracked
data/.health
data/backups/

# Logs
logs/

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/
.venv/
venv/

# Environment / secrets (never commit these)
.env
*.env
*.key
*.pem
*secret*
*credential*

# Wallet cohort and per-wallet data (private - NEVER commit)
# Wallet addresses are the core IP of this product. Exposing them
# lets anyone replicate the index and front-run the cohort.
config/wallets.txt
data/snapshots/

# macOS
.DS_Store

# Claude Code
.claude/

# Private planning docs (not for public repo)
LAUNCH_PLAN*.md
